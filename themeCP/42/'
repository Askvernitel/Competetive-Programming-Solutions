#include <bits/stdc++.h>

using namespace std;
void solve(){
	int n;
	cin >> n;
	int a[n];
	for(int i =0;i<n;i++){
		cin >> a[i];
	}
	int ca[n];
	for(int i = 0;i<=n;i++){
		memcpy(ca, a, sizeof(a));
		for(int j=0;j<i;j++){
			int p = i-j;
			int maxv= -1, idx = -1;

			for(int v= 0;v<n;v++){

			}
		}
	}
}
int main(){
	int tc;
	cin >> tc;

	while(tc--) solve();
}
