#include <bits/stdc++.h>

using namespace std;
void solve(){
	int n;
	cin >> n;
	int a[n];
	for(int i =0;i<n;i++){
		cin >> a[i];
	}
	set<int> s;
	for(int i =0;i<n;i++){
		if(a[i] == 0) s.insert(a[i]);
	}
	int p = *s.begin();
	p++;
	if(s.size() == 0){ 
		cout << 1 << "\n";
		cout << 1 << " " << n << '\n';
	}else if(s.size() == 1){
		cout << 2 << '\n';
		if(p == 1){ 
			cout << p << " " << p+1 << '\n';
			cout << p << " " << n-1 << '\n';
		}else if(p == n){ 
			cout << p-1 << " " << p << '\n';
			cout << p << " " << n-1 << '\n';
	 	}else{ 
			cout << p << " " << n << '\n';
			cout << p << " " << 2 << '\n';
		}
	}else{ 
		int c = *s.rbegin();
		c++;
		if(p == 1){ 
			if(c == n){
				cout << p << " " << c-2 << '\n';
				return;
			}
			cout << p << " " << c << '\n';
		}else if(p == n){ 

		}else{
			cout << p << " " << c << '\n';
		}
	}
}
int main(){
	int tc;
	cin >> tc;
	while(tc--) solve();
}
