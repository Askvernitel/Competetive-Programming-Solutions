#include <bits/stdc++.h>

using namespace std;
void solve(){
	int n;
	cin >> n;
	int a[n];
	for(int i = 0;i<n;i++){
		cin >> a[i];
	}

	sort(a, a+n);
	
	int m = (a[n-1]+a[0])/2
}
int main(){
	int tc;
	cin >> tc;

	while(tc--) solve();
}
