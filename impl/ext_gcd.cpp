#include <bits/stdc++.h>

using namespace std;

int x, y;

int gcd(int a, int b){ 
        if(!a){

                return 
        }

        return gcd(b%a, a);
}


int main(){ 
        int a, b;
        cin >> a >> b;


        gcd(a,b);
}
