#include <bits/stdc++.h>

using namespace std;


int main(){
    int k, n;

    cin >> k >> n;
    int a[k], b[n];
    for(int i=0;i<k;i++) cin >> a[i];
    for(int i=0;i<n;i++) cin >> b[i];
    set<int> s;
    for(int i=0;i<n;i++) s.insert(b[i]);
    for(int i = 0;i<n;i++){
        for(int j = 
    }
    
}
