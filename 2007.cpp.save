#include <bits/stdc++.h>

using namespace std;
void solve(){
    int l, r;

    cin >> l >> r;

    int k = r - l;
    while(l
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int tc;
    cin >> tc;
    while(tc--) solve();
}
