#include <bits/stdc++.h>

using namespace std;

void solve(){
    int n;
    cin >> n;
    int a[n];
    for(int i =0;i<n;i++) cin >> a[i];
    
    sort(a, a + n);
    int s[n];
    s[n-1] = 


}

int main(){
    int tc;
    cin >> tc;

    while(tc--) solve();

}
