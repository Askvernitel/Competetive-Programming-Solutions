#include <bits/stdc++.h>

using namespace std;

int main(){
    int n;
    cin >> n;
    set<string> s;
    int ans =0;
    for(int i =0;i<n;i++){
        string str1, str2;
        cin >> str1 >> str2;
        string res1 = str1.substr(0, 2) + str2;
        string res2 = str2+str.substr(0,2);
        if(s.find(res1) != s.end() || s.find(res2) != s.end()){
            ans++;
            continue;
        }
    
        s.insert(res1); s.insert(res2);
    }

    cout << ans;
}
